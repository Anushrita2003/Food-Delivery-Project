# Copy this file to .env and update the credentials before running the backend.
# Replace <username>, <password>, and <database> with your Atlas values.
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.aqlkp3n.mongodb.net/<database>?retryWrites=true&w=majority
PORT=4000
